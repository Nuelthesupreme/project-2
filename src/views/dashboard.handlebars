{{> privateNav}}
{{> carousel}}

<div class="flex flex-wrap justify-center">
  {{#each genres}}
  <div class="flex-auto sm:w-full md:w-1/2 lg:w-1/3 xl:w-1/3 rounded my-4 mx-4 h-auto orange-border-bottom">
    <div class="bg-white rounded-b lg:rounded-b-none lg:rounded-r p-4 flex flex-col justify-between leading-normal">
      <div class="text-gray-900 font-bold text-xl mb-2">{{name}}</div>
      <a href="/year/{{name}}"> <button class="dark-blue-background-colour text-white font-bold py-2 px-4 rounded">
          Choose {{name}}
        </button></a>
    </div>
  </div>
  {{/each}}
</div>
{{> footer }}